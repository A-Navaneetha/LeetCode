class Solution:
    def stoneGameIX(self, stones: list[int]) -> bool:
        a= [0, 0, 0]
        for x in stones:
            a[x % 3] += 1
        if a[0] % 2 == 0:
            return a[1] >= 1 and a[2] >= 1
        else:
            return abs(a[1] - a[2]) > 2
